/// <reference types="vite/client" />
/// <reference types="vite-plugin-pwa/react" />

/** Injected at build time (git SHA + build stamp); "dev" under the dev server. */
declare const __BUILD_ID__: string;
